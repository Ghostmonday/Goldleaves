import pytest

# Simplified conftest for now - the main app has import issues
# We'll test the billing models and logic without the full FastAPI app

@pytest.fixture
def sample_user_data():
    """Sample user data for testing."""
    return {
        "username": "testuser",
        "email": "test@example.com",
        "password": "SecurePassword123!"
    }